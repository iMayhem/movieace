# MovieAce VPS Resolver Configuration

# Server Port (internal, proxied by Nginx)
PORT=8080

# Nginx Proxy Base URL (where video streams are proxied through)
NGINX_PROXY_BASE=http://161.118.191.46

# Node Environment
NODE_ENV=production
